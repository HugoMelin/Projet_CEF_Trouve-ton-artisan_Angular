import { ArtisanDuMoisFilterPipe } from './artisan-du-mois-filter.pipe';

describe('ArtisanDuMoisFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ArtisanDuMoisFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
